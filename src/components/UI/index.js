import styled from "styled-components";
import { maxWidthContainer } from "./variables";

export const Container = styled.main`
    max-width: ${maxWidthContainer};
    margin-inline: auto;
    padding: 0px 48px;
`;